// import React from "react";
// import { twMerge } from "tailwind-merge";
// import { View } from "react-native";

// type ButtonIconProps = {
//   position: "left" | "middle" | "right";
//   // name: IconProps["name"];
//   size?: number;
//   color?: string;
// };

// export const ButtonIcon = ({
//   position,
//   // name,
//   size = 24,
//   color,
// }: ButtonIconProps) => {
//   const positionClasses = twMerge(
//     position === "left" ? "absolute left-4" : "",
//     position === "middle" && "",
//     // position === "right" ? "absolute right-4" : ""
//   );

//   return (
//     <View className={positionClasses}>
//       {/* <Icon name={name} width={size} height={size} fill={color} /> */}
//     </View>
//   );
// };
